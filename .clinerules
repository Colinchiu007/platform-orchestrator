# .clinerules — platform-orchestrator 特定硬约束

- rule: "orchestrator 只做路由+鉴权+编排，不放业务逻辑"
- rule: "不修改任何被引用模块的代码"
- rule: "所有模块作为库导入同进程运行，不引入多进程/微服务通信"
- rule: "新路由和中间件必须有对应的测试（TDD）"
- rule: "不引入新服务（不加 Redis、Celery、RabbitMQ）"
- rule: "不引入重框架（不用 Django ORM、SQLAlchemy，用 aiosqlite）"
- rule: "视频任务严格串行，同时只允许 1 个"
- rule: "新增功能开关必须在 feature_gates.yaml 中定义，使用 @requires_feature 装饰器"
